<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 13+ の通知表示に必要。おすすめ通知の設定をONにした時に実行時許可を求める -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Android 11+ のパッケージ可視性対策。X(旧Twitter)公式アプリを名指しで起動できるよう、
        twitter:// スキームと https(App Link)、およびパッケージ本体を <queries> に宣言する。
        これが無いと resolveActivity/setPackage が X アプリを見つけられず、
        「Xで開く」がブラウザへ落ちてしまう。
    -->
    <queries>
        <package android:name="com.twitter.android" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="twitter" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OshiXImageCollector">

        <!--
            ML Kit: インストール時に顔検出モデルをPlay Services側へ事前ダウンロードさせる。
            モデル未取得の間はFaceDetectorがUnavailableを返し、is_faceは未判定のまま
            次回の「最新を取得」時に再試行される。
        -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="face" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OshiXImageCollector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            おすすめ通知のタップ先。UIを持たず、Xアプリ（無ければブラウザ）へ中継してすぐ終了する。
            Android 12以降はBroadcastReceiver/Service経由の通知トランポリンが禁止されているため、
            「Xアプリの有無で開き方を変える」処理を挟むにはActivityである必要がある。
        -->
        <activity
            android:name=".tweet.TweetOpenActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    </application>

</manifest>
